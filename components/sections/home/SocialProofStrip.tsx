// Social Proof Strip
// No client names or testimonials exist in the foundation docket.

export default function SocialProofStrip() {
  return (
    <section
      aria-label="Social proof"
      style={{
        backgroundColor: 'var(--color-deep)',
      }}
    >
      {/* TODO: Insert social proof — source from client docket */}
    </section>
  )
}
